import styled from 'styled-components'
import * as Sg from '../../Global'

export const Cabecalho = styled.header`
  background-color: ${Sg.TemaPadrao.corSecundaria};
  color: ${Sg.TemaPadrao.corPrincipal};
  text-align: center;
  padding: 24px 0;
`
